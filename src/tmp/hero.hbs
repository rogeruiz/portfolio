{{#each main}}
<h2 class="hero__title is-active">
  I'm <a href="{{nameUrl}}">{{name}}</a>, 
  a <a href="{{roleUrl}}">{{role}}</a> 
  at <a eachef="{{placeUrl}}">{{place}}</a> 
  in NYC.
</h2>
{{/each}}
{{#each github}}
<h2 class="hero__title">
  I'm <a href="{{nameUrl}}">{{name}}</a>, 
  a serial-committer who <a href="{{roleUrl}}">{{role}}</a> 
  to <a href="{{placeUrl}}">{{place}}</a>.
</h2>
{{/each}}
{{#each twitter}}
<h2 class="hero__title">
  I'm <a href="{{nameUrl}}">{{name}}</a>, 
  an average-joe who follows <a href="{{roleUrl}}">{{role}}</a> 
  on <a href="{{placeUrl}}">{{place}}</a>.
</h2>
{{/each}}
{{#each pinboard}}
<h2 class="hero__title">
  I'm <a href="{{nameUrl}}">{{name}}</a>, 
  a hoarder of articles on <a href="{{roleUrl}}">{{role}}</a> 
  using <a href="{{placeUrl}}">{{place}}</a>.
</h2>
{{/each}}
<a href="javascript:" class="js-hero-change button">More.</a>