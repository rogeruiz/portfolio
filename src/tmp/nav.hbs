<a href="/" class="js-back-button nav-coffin__back"><</a>
<div class="nav-coffin__toggler js-coffin-toggle"></div>
<div class="nav-coffin__inner">
  <h3 class="nav__title">About</h3>
  <nav class="nav">
    <a href="{{about.main.roleUrl}}">{{about.main.name}}</a>
  </nav>
  <h3 class="nav__title">Current Work</h3>
  <nav class="nav">
    {{#if rokkan}}{{#each rokkan}}<a href="{{url}}">{{name}}</a>{{/each}}{{/if}}
    {{#if talks}}{{#each talks}}<a href="{{url}}">{{name}}</a>{{/each}}{{/if}}
    {{#if personal}}{{#each personal}}<a href="{{url}}">{{name}}</a>{{/each}}{{/if}}
    {{#if tools}}{{#each tools}}<a href="{{url}}">{{name}}</a>{{/each}}{{/if}}
  </nav>
  <h3 class="nav__title">Around The Web</h3>
  <nav class="nav">
    <a target="_blank" href="{{about.github.roleUrl}}">Github</a>
    <a target="_blank" href="{{about.twitter.roleUrl}}">Twitter</a>
    <a target="_blank" href="{{about.pinboard.roleUrl}}">Pinboard</a>
  </nav>
</div>